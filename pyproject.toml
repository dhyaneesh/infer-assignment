[project]
name = "infer-assignment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dhyaneesh", email = "dhyaneeshanand615@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "livekit>=1.1.19",
]

[project.scripts]
infer-assignment = "infer_assignment:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
